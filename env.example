# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DATABASE_URL=postgresql://postgres:Crab1202!@localhost:5432/interactivecompanies

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Frontend URL
FRONTEND_URL=http://localhost:3000

# SMTP Configuration
# Replace these with your actual SMTP credentials
SMTP_HOST=smtp.interactioncasino.com
SMTP_PORT=587
SMTP_SECURE=SSL
SMTP_USER=info@interactioncasino.com
SMTP_PASS=&,23,OrToLm
FROM_EMAIL=info@interactioncasino.com

# Optional: Fallback SMTP (Gmail example)
# FALLBACK_SMTP_HOST=smtp.gmail.com
# FALLBACK_SMTP_PORT=587
# FALLBACK_SMTP_SECURE=false
# FALLBACK_SMTP_USER=your-gmail@gmail.com
# FALLBACK_SMTP_PASS=your-app-password

# Optional: Skip SMTP test during startup
# SKIP_SMTP_TEST=true 